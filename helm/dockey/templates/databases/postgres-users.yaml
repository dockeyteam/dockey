{{- if .Values.databases.postgres.users.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-users
  namespace: {{ include "dockey.namespace" . }}
  labels:
    app: postgres-users
    {{- include "dockey.labels" . | nindent 4 }}
spec:
  serviceName: postgres-users
  replicas: 1
  selector:
    matchLabels:
      app: postgres-users
  template:
    metadata:
      labels:
        app: postgres-users
        {{- include "dockey.labels" . | nindent 8 }}
    spec:
      containers:
      - name: postgres
        image: "{{ .Values.databases.postgres.users.image.repository }}:{{ .Values.databases.postgres.users.image.tag }}"
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          value: {{ .Values.databases.postgres.users.database | quote }}
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgres-users-secret
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-users-secret
              key: password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        resources:
          {{- toYaml .Values.databases.postgres.users.resources | nindent 10 }}
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U postgres -d {{ .Values.databases.postgres.users.database }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U postgres -d {{ .Values.databases.postgres.users.database }}
          initialDelaySeconds: 5
          periodSeconds: 5
  volumeClaimTemplates:
  - metadata:
      name: postgres-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: {{ include "dockey.storageClass" . }}
      resources:
        requests:
          storage: {{ .Values.databases.postgres.users.storage.size }}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-users
  namespace: {{ include "dockey.namespace" . }}
  labels:
    app: postgres-users
    {{- include "dockey.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: postgres-users
{{- end }}
