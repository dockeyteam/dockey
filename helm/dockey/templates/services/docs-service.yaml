{{- if .Values.services.docs.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: docs-service
  namespace: {{ include "dockey.namespace" . }}
  labels:
    app: docs-service
    {{- include "dockey.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.docs.replicas }}
  selector:
    matchLabels:
      app: docs-service
  template:
    metadata:
      labels:
        app: docs-service
        {{- include "dockey.labels" . | nindent 8 }}
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: docs-service
        image: "{{ include "dockey.imageRegistry" . }}/{{ .Values.services.docs.image.repository }}:{{ .Values.services.docs.image.tag }}"
        imagePullPolicy: {{ include "dockey.imagePullPolicy" . }}
        ports:
        - containerPort: {{ .Values.services.docs.port }}
          name: http
        env:
        - name: KUMULUZEE_DATASOURCES0_CONNECTIONURL
          value: "jdbc:postgresql://postgres-docs:5432/{{ .Values.services.docs.env.databaseName }}"
        - name: KUMULUZEE_DATASOURCES0_USERNAME
          valueFrom:
            secretKeyRef:
              name: postgres-docs-secret
              key: username
        - name: KUMULUZEE_DATASOURCES0_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-docs-secret
              key: password
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: {{ .Values.services.docs.env.kafkaBootstrapServers | quote }}
        - name: KAFKA_CONSUMER_GROUP_ID
          value: {{ .Values.services.docs.env.kafkaConsumerGroupId | quote }}
        resources:
          {{- toYaml .Values.services.docs.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.docs.port }}
          {{- if .Values.services.docs.healthCheck.liveness }}
          {{- toYaml .Values.services.docs.healthCheck.liveness | nindent 10 }}
          {{- end }}
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.docs.port }}
          {{- if .Values.services.docs.healthCheck.readiness }}
          {{- toYaml .Values.services.docs.healthCheck.readiness | nindent 10 }}
          {{- else }}
          initialDelaySeconds: 60
          periodSeconds: 15
          timeoutSeconds: 20
          failureThreshold: 5
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: docs-service
  namespace: {{ include "dockey.namespace" . }}
  labels:
    app: docs-service
    {{- include "dockey.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.services.docs.port }}
    targetPort: {{ .Values.services.docs.port }}
    protocol: TCP
    name: http
  selector:
    app: docs-service
{{- end }}
