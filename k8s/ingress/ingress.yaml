apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dockey-ingress-users
  namespace: dockey
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rewrite /api/users/* to /v1/*
    nginx.ingress.kubernetes.io/rewrite-target: /v1/$2
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  tls:
  - hosts:
    - dockey.example.com
    secretName: dockey-tls
  rules:
  - host: dockey.example.com
    http:
      paths:
      - path: /api/users(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: user-service
            port:
              number: 8081
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dockey-ingress-docs
  namespace: dockey
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rewrite /api/docs/* to /v1/*
    nginx.ingress.kubernetes.io/rewrite-target: /v1/$2
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  tls:
  - hosts:
    - dockey.example.com
    secretName: dockey-tls
  rules:
  - host: dockey.example.com
    http:
      paths:
      - path: /api/docs(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: docs-service
            port:
              number: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dockey-ingress-comments
  namespace: dockey
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # Rewrite /api/comments/* to /api/*
    nginx.ingress.kubernetes.io/rewrite-target: /api/$2
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  tls:
  - hosts:
    - dockey.example.com
    secretName: dockey-tls
  rules:
  - host: dockey.example.com
    http:
      paths:
      - path: /api/comments(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: comments-service
            port:
              number: 8082
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dockey-ingress-auth
  namespace: dockey
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - dockey.example.com
    secretName: dockey-tls
  rules:
  - host: dockey.example.com
    http:
      paths:
      - path: /auth
        pathType: Prefix
        backend:
          service:
            name: keycloak
            port:
              number: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dockey-ingress-frontend
  namespace: dockey
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - dockey.example.com
    secretName: dockey-tls
  rules:
  - host: dockey.example.com
    http:
      paths:
      # Frontend (catch-all for React Router)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
