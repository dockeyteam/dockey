apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockey-app.fullname" . }}
  labels:
    {{- include "dockey-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dockey-app.selectorLabels" . | nindent 4 }}
