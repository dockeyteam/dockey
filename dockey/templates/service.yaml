apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockey.fullname" . }}
  labels:
    {{- include "dockey.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dockey.selectorLabels" . | nindent 4 }}
