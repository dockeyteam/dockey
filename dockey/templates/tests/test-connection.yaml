apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dockey.fullname" . }}-test-connection"
  labels:
    {{- include "dockey.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dockey.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
